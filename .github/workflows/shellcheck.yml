# checkout bring your file to the area for testing
name: sytax checker
on:
  pull_request:
    branches: [master]

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: shell check
        uses: ludeeus/action-shellcheck@master

